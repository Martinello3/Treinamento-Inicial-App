<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apptreino.meutreinamento">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/panda_vermelho"
        android:label="@string/app_name"
        android:roundIcon="@drawable/panda_vermelho"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".ui.login.LoginActivity" android:screenOrientation="locked" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.CadastroActivity" android:screenOrientation="locked"/>
        <activity android:name=".ui.activity.SplashScreenActivity" android:screenOrientation="locked"/>
        <activity android:name=".ui.activity.InfoActivity" android:screenOrientation="locked"/>
        <activity android:name=".ui.activity.AlteraActivity" android:screenOrientation="locked" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        </application>
</manifest>